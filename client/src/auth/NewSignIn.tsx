export default function NewSignIn() {
  return (
    <div style={{ minHeight: "100vh", display: "grid", placeItems: "center", color: "#fff", backgroundColor: "#000" }}>
      <div>
        <h1>Sign in</h1>
        <p>Login screen loaded successfully.</p>
        <p>This confirms the routing and component mounting works.</p>
      </div>
    </div>
  );
}