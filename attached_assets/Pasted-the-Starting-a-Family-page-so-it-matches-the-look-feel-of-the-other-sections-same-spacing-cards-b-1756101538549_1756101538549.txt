the Starting a Family page so it matches the look/feel of the other sections (same spacing, cards, buttons, security band, testimonials, gradient footer, etc.).

Starting a Family — implementation checklist
1) Hero block

Height & spacing: set hero to min-height: 420px; padding-block: 48px;

Image framing: use 4:3 image, border-radius: 12px; overflow: hidden;

Title stack width: clamp to max-width: 620px;

CTAs: primary = gold, secondary link with chevron.

<section class="hero hero--narrow">
  <div class="hero__inner container">
    <h1>FamilyVault for Starting a Family</h1>
    <p>This chapter of your life changes everything — and the Family Operating System® keeps you organized…</p>
    <div class="hero__ctas">
      <a class="btn btn--gold" href="#get-started">Get started free</a>
      <a class="link-chevron" href="#features">See family planning features</a>
    </div>
    <figure class="hero__media">
      <img src="/img/starting-family-hero.jpg" alt="New parents holding a baby on a couch">
    </figure>
  </div>
</section>

2) Feature cards grid (the 6 cards under “How FamilyVault Helps…”)

Make these the same component used on Child Info/Home Buying:

Grid: 3-up on desktop, 2-up tablet, 1-up mobile.
grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px 20px;

Card height: fixed min-height: 160px; so rows align.

Icon chip: 32×32 circle, background: rgba(255, 214, 10, .12); color: #ffd60a;

Title size: font-size: 16px; font-weight: 600;

Body: opacity: .8;

Action link: Add ___ → with chevron, same gold hover as other pages.

Applies to:

Pregnancy & Medical Records

Baby Documentation

Childcare Information

Guardianship Planning

Legal Documents

Insurance & Benefits

3) Feature rows (four “Carry/Ensure/Find/Prepare” sections)

Unify with the alternating rows used on other pages:

Layout: two-column; text col max-width: 560px; media col 4:3 mock card, border-radius: 12px; background: #0f0f0f;

Spacing: padding-block: 56px; (desktop) / 36px (mobile)

Icon seed: tiny gold glyph in a 28px circle before each H3, same as other pages.

Copy style: H3 20/28, body 14/22, one short lead sentence + 2–3 bullets max.

4) “Meet FamilyVault — Your Life, Organized” band

Use the same 3-tile band component as elsewhere:

Tile card width: 320px; border-radius: 14px;

Captions: single line title + one-line help text.

CTA button spacing: margin-block-end: 24px;

5) Security band

Exactly the shared security module:

4 icons in a row, each with small title + one line copy.

Keep compliance badges row beneath (EU GDPR, SOC 2 TYPE II, SOC 3, HIPAA, CCPA).

6) Testimonials

Use the same testimonial card component:

Width: 340–360px; border-radius: 12px; background: #121212;

Stars: 5 small gold stars row.

Avatar circle (even if placeholder) centered under quote.

Quote length constraint: 160–220 chars.

7) Footer CTA gradient

The closing CTA should match the other pages:

Gradient: linear-gradient(180deg, #111 0%, #0b0b0b 60%, #0a0a0a 100%)

Center-aligned copy + btn--gold.

Visual tokens (drop into your global CSS once)

These keep the theme consistent across all subpages.

:root{
  --brand-gold:#FFD60A;
  --gold-ink:#1a1a1a;
  --ink-1:#eaeaea;
  --ink-2:#b8b8b8;
  --bg-1:#0c0c0c;          /* page */
  --bg-2:#101010;          /* cards */
  --ring:rgba(255,214,10,.3);
  --radius-12:12px;
}

/* Buttons */
.btn{display:inline-flex;align-items:center;gap:.5rem;padding:.7rem 1rem;border-radius:999px;font-weight:600;}
.btn--gold{background:var(--brand-gold);color:var(--gold-ink);}
.btn--gold:hover{filter:brightness(1.05);}

/* Link with chevron */
.link-chevron{color:var(--ink-1);font-weight:600}
.link-chevron::after{content:"→";margin-left:.4rem;color:var(--brand-gold);}

/* Cards */
.k-card{background:var(--bg-2);border:1px solid rgba(255,255,255,.06);border-radius:var(--radius-12);padding:16px;}
.k-card__icon{width:32px;height:32px;border-radius:999px;display:grid;place-items:center;
  background:rgba(255,214,10,.12);color:var(--brand-gold);}

/* Section spacing */
.section{padding-block:56px}
@media(max-width:900px){.section{padding-block:36px}}

/* Two-col feature rows */
.feature{display:grid;grid-template-columns:1.1fr .9fr;gap:28px;align-items:center;}
@media(max-width:980px){.feature{grid-template-columns:1fr;}}
.feature__media{border-radius:12px;overflow:hidden;background:#0f0f0f;border:1px solid rgba(255,255,255,.06);}
.feature__media img{display:block;width:100%;height:auto;aspect-ratio:4/3;object-fit:cover;}