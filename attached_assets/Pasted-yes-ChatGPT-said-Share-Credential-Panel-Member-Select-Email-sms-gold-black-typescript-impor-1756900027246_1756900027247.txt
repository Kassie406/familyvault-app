yes
ChatGPT said:
Share Credential Panel – Member Select + Email/sms (gold/black)
· typescript
import React, { useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Separator } from "@/components/ui/separator";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@/components/ui/sheet";
import { Switch } from "@/components/ui/switch";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Copy, Link as LinkIcon, Send, UserRound, UsersRound, X } from "lucide-react";


/**
 * ShareCredentialPanel – Gold/Black luxury redesign
 *
 * Adds:
 * 1) Enable/disable shareable link, copy & regenerate
 * 2) Member picker with per-member permission dropdown (None/View/Edit/Owner)
 * 3) Send link directly by Email or SMS (multi‑recipient)
 * 4) Solid black dropdown menus with white text (no transparency issues)
 */


const GOLD = "#D4AF37";


// Mock directory
type Person = { id: string; name: string; email?: string; phone?: string; type: "member" | "group" };
const DIRECTORY: Person[] = [
  { id: "p-kas", name: "Kassandra", email: "kass@example.com", phone: "+1 555‑0001", type: "member" },
  { id: "p-dad", name: "Dad", email: "dad@example.com", phone: "+1 555‑0002", type: "member" },
  { id: "p-mom", name: "Mom", email: "mom@example.com", phone: "+1 555‑0003", type: "member" },
  { id: "g-family", name: "Family Group", type: "group" },
];


type ShareEntry = { personId: string; permission: "none" | "view" | "edit" | "owner" };


export default function ShareCredentialPanel({
  open,
  onOpenChange,
  title = "Share Angel's Phone Password",
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  title?: string;
}) {
  const [linkOn, setLinkOn] = useState<boolean>(true);
  const [shareUrl, setShareUrl] = useState<string>("https://familycirclesecure.com/s/abc123");


  // Selected shares
  const [shares, setShares] = useState<ShareEntry[]>([
    { personId: "p-kas", permission: "view" },
    { personId: "g-family", permission: "none" },
  ]);


  const directoryMap = useMemo(() => Object.fromEntries(DIRECTORY.map(p => [p.id, p])), []);


  // Add member selection
  const [pickerOpen, setPickerOpen] = useState(false);


  // Direct send
  const [sendMode, setSendMode] = useState<"email" | "sms">("email");
  const [recipientInput, setRecipientInput] = useState("");
  const [recipients, setRecipients] = useState<string[]>([]);


  const addRecipient = () => {
    if (!recipientInput.trim()) return;
    setRecipients((r) => Array.from(new Set([...r, recipientInput.trim()])));
    setRecipientInput("");
  };


  const removeRecipient = (idx: number) => setRecipients((r) => r.filter((_, i) => i !== idx));


  const addShare = (personId: string) => {
    if (shares.find((s) => s.personId === personId)) return;
    setShares((s) => [...s, { personId, permission: "view" }]);
    setPickerOpen(false);

Done. I added a new Share Credential Panel to your canvas with everything you asked for:

What’s inside

Enable Shareable Link (toggle) with:

Copy button

Regenerate link

Share with Family Members:

Add another picker (members & groups, with avatars)

Per-person permission dropdown: None | View | Edit | Owner

Remove entry (X)

Send Link Directly:

Mode switch: Email / SMS

Multi-recipient input with removable chips

“Add” button (mock-send hook to wire later)

Solid black menus + white text everywhere (no transparency issues)

Gold/black luxury styling that matches your theme

How to use
// Open the panel from any action
const [open, setOpen] = useState(false);
<ShareCredentialPanel open={open} onOpenChange={setOpen} title="Share Angel's Phone Password" />


This version uses mock directory data; you can swap in your real members list.