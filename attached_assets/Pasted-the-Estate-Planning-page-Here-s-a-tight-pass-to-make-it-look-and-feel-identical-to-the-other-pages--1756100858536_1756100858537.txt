the Estate Planning page. Here’s a tight pass to make it look and feel identical to the other pages (color tokens, spacing, cards, pills, testimonials, and CTA all match your shared patterns).

What to fix (quick list)

Theme tokens (use the same everywhere)

Text on dark: #EDEDED

Subtext: #9BA3AF

Surfaces: #0F0F10, cards #151517

Stroke: rgba(255,255,255,.08)

Gold brand: #E7C74D

Gold soft bg: rgba(231, 199, 77, .08)

Focus ring: #8EC8FF

Hero area

Standardize H1 size/weight and the eyebrow “Estate Planning” pill.

Make the media the same rounded rectangle (16px) with subtle 1px stroke.

Top features grid (6 cards)

Equal heights, consistent 16px radius, 1px low-contrast stroke, gold icon chip.

Hover: lift + subtle glow using gold-soft.

Pill row (“Will and trust storage”, etc.)

Convert to the same “feature-pills” component used on other pages (height 40px, icon left, gold outline on hover).

Mid-page feature sections

Keep the alternating two-column layout used on other pages (image/card left, copy right, and vice versa). Match gutter (clamp 24–40px) and max-width.

“Meet FamilyVault” strip

Use the same 3 tiles and icon sizes as Child Info / Moving / Disaster pages for consistency.

Security block

Reuse the global security component (identical spacing + icon row). Avoid variant font sizes.

Testimonials

Use the same testimonial card style and carousel when >3. (The three you have can sit static, but match padding and star row exactly.)

Bottom CTA

Use the same subtle top-to-bottom warm gradient + centered CTA button with gold fill and dark focus ring.

A11y + motion

Add focus rings on all interactive elements.

Reduce motion for users with prefers-reduced-motion.

Drop-in CSS (paste into your page’s sheet after variables)
/* === THEME TOKENS (keep identical across pages) === */
:root{
  --bg: #0F0F10;
  --card: #151517;
  --text: #EDEDED;
  --muted: #9BA3AF;
  --stroke: rgba(255,255,255,.08);
  --gold: #E7C74D;
  --gold-soft: rgba(231,199,77,.08);
  --ring: #8EC8FF;
  --radius: 16px;
  --shadow: 0 6px 28px rgba(0,0,0,.35);
}

/* Page shell */
.page-ep { background: var(--bg); color: var(--text); }
.page-ep .container { max-width: 1120px; margin: 0 auto; padding-inline: clamp(16px, 3vw, 24px); }

/* Eyebrow pill */
.eyebrow {
  display:inline-flex; align-items:center; gap:8px;
  padding:6px 10px; border-radius:999px;
  background: var(--gold-soft); color: var(--gold); border:1px solid var(--stroke);
  font-size:12px; font-weight:600; letter-spacing:.02em;
}

/* Hero */
.hero { display:grid; grid-template-columns: 1.1fr .9fr; gap: clamp(24px, 4vw, 40px); align-items:center; }
.hero h1 { font-size: clamp(28px, 4vw, 40px); line-height:1.1; letter-spacing:-.01em; }
.hero p { color: var(--muted); max-width: 58ch; }
.hero-media {
  border-radius: var(--radius); overflow:hidden; position:relative; outline:1px solid var(--stroke);
  box-shadow: var(--shadow);
}
.hero-media img { display:block; width:100%; height:auto; }

/* CTA button */
.btn-gold {
  background: var(--gold); color:#1A1A1B; font-weight:700;
  padding:12px 18px; border-radius:999px; border:0; box-shadow:none; transition: transform .08s ease;
}
.btn-gold:hover { transform: translateY(-1px); }
.btn-gold:focus-visible { outline: 3px solid var(--ring); outline-offset:2px; }

/* Six-card feature grid */
.features-grid {
  display:grid; grid-template-columns: repeat(3, 1fr);
  gap: clamp(12px, 2.5vw, 20px); margin-top: 28px;
}
@media (max-width: 900px){ .features-grid { grid-template-columns: 1fr; } }

.feature-card {
  display:flex; flex-direction:column; gap:12px; min-height: 172px;
  background: var(--card); border:1px solid var(--stroke); border-radius: var(--radius);
  padding:16px 16px 18px; transition: box-shadow .2s ease, border-color .2s ease, transform .2s ease;
}
.feature-card:hover {
  border-color: var(--gold-soft);
  box-shadow: 0 0 0 1px var(--gold-soft), 0 10px 30px rgba(0,0,0,.35);
  transform: translateY(-1px);
}

.icon-chip {
  width:36px; height:36px; border-radius:10px;
  display:grid; place-items:center;
  background: var(--gold-soft); color: var(--gold);
  outline:1px solid var(--stroke);
}

/* Inline links */
.inline-link { color: var(--text); text-decoration: none; border-bottom:1px solid var(--stroke); }
.inline-link:hover { border-bottom-color: var(--gold); color: var(--gold); }

/* Feature pills row (under grid) */
.feature-pills { display:flex; flex-wrap:wrap; gap:10px; margin-block: 18px 8px; }
.pill {
  display:inline-flex; align-items:center; gap:8px; height:40px;
  padding:0 14px; border-radius:999px; border:1px solid var(--stroke);
  color: var(--muted); background: transparent;
}
.pill:hover { border-color: var(--gold); color: var(--gold); }

/* Alternating 2-col sections */
.section-alt { display:grid; grid-template-columns: 1fr 1fr; gap: clamp(20px, 3.2vw, 36px); align-items:center; }
.section-alt .visual {
  background: var(--card); border:1px solid var(--stroke); border-radius: var(--radius);
  padding:14px; min-height: 220px; box-shadow: var(--shadow);
}
.section-alt .copy p { color: var(--muted); }
@media (max-width: 900px){ .section-alt { grid-template-columns: 1fr; } }

/* Testimonial cards */
.testimonials { display:grid; grid-template-columns: repeat(3, 1fr); gap: clamp(12px, 2.5vw, 20px); }
@media (max-width: 900px){ .testimonials { grid-template-columns: 1fr; } }

.testimonial {
  background: var(--card); border:1px solid var(--stroke); border-radius: var(--radius); padding:16px;
}
.testimonial .stars { color: var(--gold); }

/* Bottom CTA strip */
.cta-strip {
  background: linear-gradient(180deg, rgba(231,199,77,.07) 0%, rgba(231,199,77,0) 60%);
  border-top: 1px solid var(--stroke);
  padding: clamp(24px, 5vw, 48px) 0;
  text-align:center;
}

/* Security block (shared) */
.security { border-top:1px solid var(--stroke); padding-top: 28px; }

/* Focus & reduced motion */
*:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; }
@media (prefers-reduced-motion: reduce){
  .btn-gold, .feature-card { transition: none !important; }
}

Minimal HTML alignment (examples)

Hero snippet

<section class="hero container">
  <div>
    <span class="eyebrow">Estate Planning</span>
    <h1>FamilyVault for Estate Planning</h1>
    <p>Secure and organize your vital information so your loved ones have what they need, when they need it most.</p>
    <div style="margin-top:16px;">
      <button class="btn-gold">Get started free</button>
      <a class="inline-link" href="#features" style="margin-left:14px;">See estate planning features →</a>
    </div>
  </div>

  <figure class="hero-media">
    <img src="/assets/estate-hero.jpg" alt="Estate planning documents and gavel" loading="eager">
  </figure>
</section>


Six-card grid (keep your existing content, just add classes)

<section id="features" class="container" style="margin-top:28px;">
  <div class="features-grid">
    <article class="feature-card">
      <div class="icon-chip">⚖️</div>
      <h3>Wills & Trusts</h3>
      <p class="muted">Store your will, trust documents, and beneficiary information securely.</p>
      <a class="inline-link" href="#">Add documents →</a>
    </article>
    <!-- repeat for the other 5 cards -->
  </div>

  <div class="feature-pills">
    <span class="pill">⚖️ Will and trust storage</span>
    <span class="pill">🩺 Healthcare directive access</span>
    <span class="pill">📄 Power of attorney documents</span>
    <span class="pill">👩‍⚖️ Professional contact sharing</span>
  </div>
</section>


Alternating sections

<section class="container section-alt" style="margin-top:28px;">
  <div class="visual"><!-- your “Will card” visual --></div>
  <div class="copy">
    <h3>Store and Share Estate Plans</h3>
    <p>Set up legacy access so your family knows where to find key documents after your passing.</p>
  </div>
</section>

<section class="container section-alt" style="margin-top:20px;">
  <div class="copy">
    <h3>Organize Medical Directives</h3>
    <p>Keep your living will and advance directives accessible to family and providers.</p>
  </div>
  <div class="visual"><!-- your “Medical directive” card --></div>
</section>


Testimonials (match site-wide style)

<section class="container" style="margin-top:32px;">
  <h2>What Our Members Say About FamilyVault</h2>
  <div class="testimonials">
    <article class="testimonial">
      <div class="stars">★★★★★</div>
      <p>“Having all our estate documents organized in FamilyVault gives our family such peace of mind.”</p>
      <small>Patricia H., Member since 2022</small>
    </article>
    <!-- two more… -->
  </div>
</section>


Bottom CTA

<section class="cta-strip">
  <div class="container">
    <h3>Secure your family’s future today.</h3>
    <p class="muted">Start organizing your estate planning documents with FamilyVault.</p>
    <button class="btn-gold" style="margin-top:10px;">Get started free</button>
  </div>
</section>